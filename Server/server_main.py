#python3 server script
import os
from datetime import datetime


def printfile(message):

#update [client]_command.py to send command
def command(message, client):

#read [client]_data.py to get data
def get_data(client):


#main loop
while True:
	cycle_count +=1


#error message
error_message = "main loop terminated, with " + str(cycle_count) + " successful cycles"
printfile(error_message)